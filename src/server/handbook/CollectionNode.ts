import { HandbookNode } from './HandbookNode';
import { ProcessingState } from './types';

export class CollectionNode extends HandbookNode {
  process(state: ProcessingState): void {
    return;
  }
}
